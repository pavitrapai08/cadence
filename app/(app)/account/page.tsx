import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function AccountPage() {
  return (
    <PagePlaceholder
      title="Account"
      description="Profile, notification preferences, and workspace settings (admin)."
      phase="Phase 5"
    />
  );
}
