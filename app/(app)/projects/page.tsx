import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Project dashboard and billable / non-billable breakdowns."
      phase="Phase 3"
    />
  );
}
